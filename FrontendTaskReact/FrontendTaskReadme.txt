Create a vue.js web app to render the orders table with search filters (see reference screenshot).

Task specifications:
- Vue
- Vue Router

Requirements:
- Need to create a route with /orders
- In orders page: you need to create a table similar to screenshot attached with search filter on top of it.
- Search should be working as (List of orders in table should be filtered with order number and customer name is the list)
- Make the vuejs source code, along with necessary dependency requirements (e.g. package.json), available to us somehow, either on Github, Google Drive ZIP, emailed ZIP, etc.  Do not include the "node_modules" directory.